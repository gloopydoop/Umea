function R_eff = calc_effective_r(x,y,eps)
i_start = 0;
i_stop = 0;
for i = 1:size(x)
    if i_stop == 0
        if i_start == 0 && x(i) < 1 - eps
            i_start = i;
        end
        
     
        
    